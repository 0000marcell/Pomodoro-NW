
import { pomodoroHours } from 'pomodoro-electron/helpers/pomodoro-hours';
import { module, test } from 'qunit';

module('Unit | Helper | pomodoro hours');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = pomodoroHours([42]);
  assert.ok(result);
});

