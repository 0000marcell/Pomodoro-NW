App.StatisticsRoute = Ember.Route.extend({
  actions: {
    didTransition(){
      //let controller = App.__container__.lookup("controller:application"); 
      //controller.set('showClock', false);
    }
  }
});

